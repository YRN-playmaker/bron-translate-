# Copy to .env and fill in. NEVER commit the real .env.

# LLM translation API
LLM_API_KEY=

# Cloud ASR API
CLOUD_ASR_API_KEY=

# Payment webhook verification
PAYMENT_WEBHOOK_TOKEN=

# Admin endpoint key — generate a strong random value:
#   python -c "import secrets; print(secrets.token_hex(32))"
ADMIN_KEY=
